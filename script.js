const toggle=document.querySelector('.menu-toggle');const nav=document.querySelector('.desktop-nav');if(toggle&&nav){toggle.addEventListener('click',()=>{const open=nav.classList.toggle('open');toggle.setAttribute('aria-expanded',String(open))});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')))}
function mailtoSubmit(form,subject,noteId,kind){form.addEventListener('submit',e=>{e.preventDefault();const d=new FormData(form);let body='';if(kind==='project'){body=`Name: ${d.get('name')}\nEmail: ${d.get('email')}\nService: ${d.get('service')}\n\nProject details:\n${d.get('details')}`;}else{body=`Name: ${d.get('name')}\nBusiness: ${d.get('business')||'N/A'}\nEmail: ${d.get('email')}\nService: ${d.get('service')}\nRating: ${d.get('rating')}/5\n\nReview:\n${d.get('review')}\n\nPermission to publish: Yes`;}const url=`mailto:contact@ederito.com?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`;window.location.href=url;const note=document.getElementById(noteId);if(note){note.textContent='Your email app should open now. Send the message to complete your submission.';note.classList.add('form-success')}})}
const projectForm=document.getElementById('projectForm');if(projectForm)mailtoSubmit(projectForm,'New EDERITO project request','formNote','project');const reviewForm=document.getElementById('reviewForm');if(reviewForm)mailtoSubmit(reviewForm,'New EDERITO client review','reviewNote','review');
