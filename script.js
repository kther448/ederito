const page=document.body.dataset.page||'';const nav=[['Home','index.html','home'],['Services','services.html','services'],['Work','work.html','work'],['Pricing','pricing.html','pricing'],['Reviews','reviews.html','reviews'],['About','about.html','about'],['Contact','contact.html','contact']];const header=document.getElementById('siteHeader');if(header){header.className='site-header';header.innerHTML=`<a class="brand" href="index.html" aria-label="EDERITO home"><img src="assets/ederito-logo.svg" alt="EDERITO"></a><nav class="site-nav" id="siteNav" aria-label="Primary navigation">${nav.map(([label,href,key])=>`<a href="${href}" class="${page===key?'active':''}">${label}</a>`).join('')}</nav><a class="header-contact" href="contact.html">Start a project</a><button class="menu-button" id="menuButton" aria-expanded="false" aria-controls="siteNav" aria-label="Open menu"><span></span><span></span><span></span></button>`}const mb=document.getElementById('menuButton'),sn=document.getElementById('siteNav');if(mb&&sn){mb.addEventListener('click',()=>{const open=sn.classList.toggle('open');mb.setAttribute('aria-expanded',String(open))});sn.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{sn.classList.remove('open');mb.setAttribute('aria-expanded','false')}))}const footer=document.getElementById('siteFooter');if(footer){footer.className='site-footer';footer.innerHTML=`<div><img src="assets/ederito-logo.svg" alt="EDERITO"><p>Independent design and digital support for small businesses.</p></div><div><a href="services.html">Services</a><a href="work.html">Selected work</a><a href="pricing.html">Pricing</a><a href="reviews.html">Reviews</a></div><div><a href="mailto:contact@ederito.com">contact@ederito.com</a><a href="privacy.html">Privacy</a><a href="terms.html">Terms</a><span>Operated by Zanara Labs LLC</span><span>© ${new Date().getFullYear()} Zanara Labs LLC</span></div>`}async function postForm(form,endpoint,status){const btn=form.querySelector('button[type=submit]');btn.disabled=true;status.className='form-status';status.textContent='Sending...';const data=Object.fromEntries(new FormData(form).entries());try{const r=await fetch(endpoint,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(data)});const j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j.error||'Something went wrong.');status.classList.add('success');status.textContent=j.message||'Sent successfully.';form.reset()}catch(e){status.classList.add('error');status.textContent=e.message||'Unable to send right now.'}finally{btn.disabled=false}}const contactForm=document.getElementById('contactForm');if(contactForm){contactForm.addEventListener('submit',e=>{e.preventDefault();postForm(contactForm,'/api/contact',document.getElementById('contactStatus'))})}const reviewForm=document.getElementById('reviewForm');if(reviewForm){reviewForm.addEventListener('submit',e=>{e.preventDefault();postForm(reviewForm,'/api/reviews',document.getElementById('reviewStatus'))})}async function loadReviews(){const wall=document.getElementById('reviewWall');if(!wall)return;try{const r=await fetch('/api/reviews');if(!r.ok)throw new Error();const j=await r.json();if(!j.reviews?.length){wall.innerHTML='<p class="muted">Published client reviews will appear here after approval.</p>';return}wall.innerHTML=j.reviews.map(x=>`<article class="review-item"><div class="stars">${'★'.repeat(Number(x.rating)||5)}</div><blockquote>“${escapeHtml(x.review)}”</blockquote><footer>${escapeHtml(x.name)}${x.business?' — '+escapeHtml(x.business):''} · ${escapeHtml(x.service||'Client')}</footer></article>`).join('')}catch{wall.innerHTML='<p class="muted">Reviews are temporarily unavailable.</p>'}}function escapeHtml(v=''){return String(v).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}loadReviews();